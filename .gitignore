# research PDFs: large, and mostly publisher-licensed. Unanchored: the
# directory has lived at the repo root and under docs/, and 23MB of
# licensed PDFs must not become committable by being moved.
papers/

# secrets. .env.example is the committed template.
.env
.local.env
*.env
!.env.example

# data that must never leave the device
/data/
/eval/photos/
/eval/results/
/eval/truth.csv
/eval/dinnerware.json

# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# macos
.DS_Store

# Symlinks into the primary checkout when this is a git worktree.
/data
/docs/papers
/eval/photos
