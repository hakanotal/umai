"""Record the first real Health Auto Export payload, then replay it forever.

Gives the real payload shape rather than what the docs claim, which is
usually a small but expensive difference.

Usage: --record | --replay tests/fixtures/health_YYYY-MM-DD.json
"""
